# UCAgent 计划管理函数作用分析报告

## 概述

UCAgent 是一个基于大语言模型的自动化硬件验证 AI 代理，专注于芯片设计的单元测试验证工作。在智能体执行过程中，计划管理函数（如 `CreateToDo`、`CompleteToDoSteps` 等）扮演着**任务分解、进度跟踪和执行组织**的核心角色。

## 一、计划管理函数概览

### 1.1 核心函数

UCAgent 在 `vagent/tools/planning.py` 模块中提供了以下计划管理工具：

| 函数名 | 功能描述 | 主要参数 |
|--------|----------|----------|
| `CreateToDo` | 创建新的待办任务列表 | `task_description`（任务描述）<br/>`steps`（步骤列表） |
| `CompleteToDoSteps` | 标记步骤为已完成 | `completed_steps`（已完成步骤索引）<br/>`notes`（备注） |
| `UndoToDoSteps` | 撤销已完成的步骤 | `steps`（需要撤销的步骤索引）<br/>`notes`（备注） |
| `ResetToDo` | 重置当前待办列表 | 无参数 |
| `GetToDoSummary` | 获取待办列表摘要 | 无参数 |
| `ToDoState` | 显示待办列表当前状态 | 无参数（动态更新描述） |

### 1.2 数据结构

**ToDoPanel** 类负责管理待办事项，维护以下核心数据结构：

```python
{
    'task_description': str,        # 当前任务描述
    'steps': [[step_desc, is_completed], ...],  # 步骤列表及完成状态
    'created_at': str,              # 创建时间
    'updated_at': str,              # 更新时间
    'notes': str                    # 备注信息
}
```

## 二、在智能体执行中的作用

### 2.1 三种交互模式下的应用

UCAgent 支持三种智能体交互模式，计划管理函数在不同模式下的启用策略不同：

#### 1. **Standard 模式（标准模式）**
- **启用条件**：需要显式设置 `--use-todo-tools` 或 `--force-todo` 参数
- **作用**：提供基本的任务规划能力，但不强制使用
- **代码位置**：`vagent/verify_agent.py:187-195`

```python
if (interaction_mode == "standard" and force_todo) or use_todo_tools:
    self.planning_tools = [
        CreateToDo(self.todo_panel),
        CompleteToDoSteps(self.todo_panel),
        UndoToDoSteps(self.todo_panel),
        ResetToDo(self.todo_panel),
        GetToDoSummary(self.todo_panel),
        ToDoState(self.todo_panel),
    ]
```

#### 2. **Enhanced 模式（增强模式）**
- **启用条件**：设置 `--interaction-mode enhanced` 参数
- **作用**：深度集成计划管理，引导智能体进行结构化任务分解和执行
- **特点**：
  - 自动引导智能体创建详细计划
  - 跟踪执行阶段和进度
  - 提供上下文感知的工具推荐
- **代码位置**：`vagent/interaction/enhanced.py`

#### 3. **Advanced 模式（高级模式）**
- **启用条件**：设置 `--interaction-mode advanced` 参数
- **作用**：在 Enhanced 基础上增加自适应策略
- **代码位置**：`vagent/interaction/`

### 2.2 在验证工作流中的角色

UCAgent 的验证工作流包含多个阶段（stages），计划管理函数在各阶段发挥以下作用：

```
┌─────────────────────────────────────────────────────────┐
│              UCAgent 验证工作流                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  阶段 1: 需求分析与规划 (requirement_analysis)           │
│  ├─ CreateToDo: 分解验证任务为具体步骤                   │
│  ├─ 使用工具获取文档和上下文                              │
│  └─ 保存规划结果到记忆系统                                │
│                                                         │
│  阶段 2-N: 执行各验证阶段                                │
│  ├─ GetToDoSummary: 查看当前任务进度                     │
│  ├─ 根据计划执行具体步骤                                  │
│  ├─ CompleteToDoSteps: 标记完成的步骤                    │
│  └─ 必要时 ResetToDo 调整计划                            │
│                                                         │
│  反思与调整                                              │
│  ├─ 定期检查计划完成情况                                  │
│  ├─ UndoToDoSteps: 撤销需要重做的步骤                    │
│  └─ 基于执行结果优化后续步骤                              │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 2.3 Enhanced 模式的执行流程

在 Enhanced 交互模式下，计划管理函数与智能体执行深度集成：

#### **规划阶段（Planning Phase）**
```python
# vagent/interaction/enhanced.py:196-228
def _create_planning_message(self, current_tips: str) -> str:
    planning_prompt = f"""
    PLANNING PHASE - Let's create a detailed plan for the current subtask.
    
    1. ANALYZE THE TASK:
       - Use SemanticSearchInGuidDoc to search for relevant documentation
       - Understand requirements and constraints
    
    2. CREATE A DETAILED PLAN:
       - Use CreateToDo to create a structured plan with specific steps
       - Break down the task into manageable steps
       - Define expected outputs
    
    3. GATHER INFORMATION:
       - Use MemoryGet to retrieve relevant context
       - Review files using ReadTextFile
    
    4. SAVE INSIGHTS:
       - Use MemoryPut to save planning insights
    """
```

**作用**：
- 强制智能体在开始任务前创建结构化计划
- 确保任务被分解为可管理的步骤
- 将规划思路记录到记忆系统

#### **执行阶段（Execution Phase）**
```python
# vagent/interaction/enhanced.py:230-250
def _create_execution_message(self, plan_status: str) -> str:
    execution_prompt = f"""
    EXECUTION PHASE - Execute the planned steps systematically.
    
    1. FOLLOW THE PLAN:
       - Execute the next incomplete step
       - Use CompleteToDoSteps to mark completed steps
       - If issues arise, consider ResetToDo for new approach
    
    2. USE TOOLS STRATEGICALLY:
       - File tools for reading/writing
       - Check tools for verification
    """
```

**作用**：
- 提供当前计划状态给智能体
- 指导智能体按计划执行
- 跟踪和记录执行进度

#### **反思阶段（Reflection Phase）**
```python
# vagent/interaction/enhanced.py:167-179
def _handle_reflection_phase(self):
    """Handle the reflection phase for self-assessment"""
    # 创建反思消息
    reflection_message = self._create_reflection_message()
    # 执行反思
    self._execute_with_message(reflection_message)
    # 重置到规划或执行阶段
    self.state.transition_to_phase("execution")
```

**作用**：
- 定期评估执行效果
- 必要时调整计划
- 学习和优化执行策略

## 三、工具编排和上下文感知

### 3.1 工具分类系统

计划管理函数被归类为 **PLANNING** 类别工具（`vagent/interaction/orchestrator.py:51-53`）：

```python
ToolCategory.PLANNING: [
    'CreateToDo', 'CompleteToDoSteps', 'UndoToDoSteps', 
    'ResetToDo', 'GetToDoSummary'
]
```

### 3.2 上下文映射

在不同的执行上下文中，系统会推荐使用计划管理工具：

| 执行上下文 | 推荐的工具类别 | 典型场景 |
|-----------|--------------|---------|
| `planning_phase` | PLANNING + SEARCH + MEMORY | 开始新任务，需要制定计划 |
| `implementation` | FILE_OPS + EXECUTION + PLANNING | 执行实现任务，跟踪进度 |
| `focused_execution` | EXECUTION + VERIFICATION + PLANNING | 集中执行特定步骤 |

### 3.3 动态描述更新

`ToDoState` 工具的描述会根据当前待办列表状态动态更新：

```python
# vagent/tools/planning.py:32-40
def set_tool_process(self):
    if self._empty():
        self.set_stat_tool_des("No Active ToDo List.")
    elif self._is_all_completed():
        self.set_stat_tool_des("Current ToDo list is completed! You can create a new one.")
    else:
        cmp_count = sum(is_cmp for _, is_cmp in self.todo_list['steps'])
        un_cmp_count = len(self.todo_list['steps']) - cmp_count
        self.set_stat_tool_des(
            f"Current ToDo List has {len(self.todo_list['steps'])} steps, "
            f"{cmp_count} completed and {un_cmp_count} uncompleted."
        )
```

这样智能体可以通过工具描述快速了解当前计划状态，无需显式调用工具。

## 四、实际应用场景示例

### 场景 1：硬件验证任务分解

**任务**：为一个加法器（Adder）DUT 创建完整的单元测试

**使用计划管理函数的流程**：

1. **创建计划**（CreateToDo）：
```python
task_description = "为 Adder DUT 创建完整的单元测试"
steps = [
    "阅读 Adder/README.md 理解 DUT 功能和接口",
    "搜索 Guide_Doc 获取测试用例编写规范",
    "设计基础功能测试用例（正常加法）",
    "设计边界测试用例（溢出、零值）",
    "设计随机测试用例",
    "编写测试代码",
    "运行测试并分析覆盖率",
    "修复发现的问题",
    "生成测试报告"
]
```

2. **执行与跟踪**：
```
- 步骤 1-2 完成后 → CompleteToDoSteps([1, 2])
- 步骤 3-5 设计完成后 → CompleteToDoSteps([3, 4, 5], notes="设计了20个测试用例")
- 步骤 6 编写代码 → CompleteToDoSteps([6])
- 步骤 7 发现问题需要重新设计 → UndoToDoSteps([3, 4], notes="需要补充边界用例")
- 重新执行步骤 3-4 → CompleteToDoSteps([3, 4])
- 继续执行剩余步骤...
```

3. **查看进度**：
```
GetToDoSummary() 返回：
-------- ToDo List --------
 Task Description: 为 Adder DUT 创建完整的单元测试
 Steps:
  1(completed): 阅读 Adder/README.md 理解 DUT 功能和接口
  2(completed): 搜索 Guide_Doc 获取测试用例编写规范
  3(completed): 设计基础功能测试用例（正常加法）
  4(completed): 设计边界测试用例（溢出、零值）
  5(completed): 设计随机测试用例
  6(completed): 编写测试代码
  7: 运行测试并分析覆盖率
  8: 修复发现的问题
  9: 生成测试报告
 Created At: 2024-11-04 10:30:15
 Updated At: 2024-11-04 11:45:22
 Notes: 设计了20个测试用例
----------------------------
```

### 场景 2：与 MCP Server 协同工作

当 UCAgent 作为 MCP Server 与外部 Code Agent（如 Qwen Code CLI）协同时：

**配置**：
```bash
ucagent output/ Adder --tui --mcp-server-no-file-tools --use-todo-tools
```

**工作流程**：

1. **Code Agent 通过 MCP 调用 UCAgent 工具**：
   - `RoleInfo()` - 获取角色和任务信息
   - `CreateToDo()` - 创建验证计划
   - `CurrentTips()` - 获取当前阶段提示

2. **Code Agent 使用自己的文件工具执行任务**：
   - 读取源代码文件
   - 编写测试用例
   - 修改测试代码

3. **定期调用 UCAgent 的计划管理工具**：
   - `GetToDoSummary()` - 检查进度
   - `CompleteToDoSteps()` - 更新完成状态
   - `Check()` - 运行测试验证

4. **任务完成**：
   - `Complete()` - 标记阶段完成
   - 进入下一个验证阶段

## 五、技术实现细节

### 5.1 步骤验证机制

```python
# vagent/tools/planning.py:66-79
def _check_str_size(self, notes, steps, emsg, info_size=10, min_steps=2, max_steps=20):
    """验证步骤数量和描述长度"""
    if len(steps) > max_steps:
        return False, f"Error: total steps({len(steps)}) exceed max_steps({max_steps})"
    if len(steps) < min_steps:
        return False, f"Error: total steps({len(steps)}) less than min_steps({min_steps})"
    if len(step) > self.max_str_size:  # 默认 100 字符
        return False, f"Error: step description too long"
    return True, ""
```

**作用**：
- 确保步骤数量合理（2-20步）
- 避免步骤描述过长影响效率
- 保持计划的可读性和可管理性

### 5.2 状态持久化

虽然当前实现中 ToDoPanel 数据存储在内存中，但 UCAgent 支持会话历史保存：

```python
# vagent/verify_agent.py:93-99
saved_info = {}
if not no_history:
    saved_info = fc.load_ucagent_info(workspace)
```

计划状态可以作为会话信息的一部分被保存到 `.ucagent_info.json` 文件中。

### 5.3 工具链集成

计划管理工具与其他工具形成完整的工具链：

```python
# vagent/verify_agent.py:196
self.test_tools = (
    self.tool_list_base +      # 基础工具（RoleInfo, ReadTextFile等）
    self.tool_list_file +      # 文件操作工具
    self.tool_list_task +      # 任务管理工具（Check, Complete等）
    self.tool_list_ext +       # 外部扩展工具
    self.planning_tools        # 计划管理工具
)
```

## 六、优势与价值

### 6.1 提升任务执行效率

- **结构化分解**：复杂验证任务被分解为清晰的步骤
- **进度可视化**：随时查看任务完成情况
- **避免遗漏**：系统化的步骤列表确保所有工作都被覆盖

### 6.2 增强智能体推理能力

- **减少认知负担**：智能体不需要记住所有待完成的任务
- **聚焦当前步骤**：明确知道下一步应该做什么
- **上下文保持**：计划作为长期上下文，减少对话历史依赖

### 6.3 支持人机协同

- **透明度**：人类可以查看智能体的计划
- **可干预性**：人类可以调整计划或标记步骤状态
- **一致理解**：人机对任务有共同的结构化理解

### 6.4 适应不同复杂度的任务

- **简单任务**（Standard 模式）：可选使用，不增加额外开销
- **中等任务**（Enhanced 模式）：自动引导创建计划
- **复杂任务**（Advanced 模式）：动态调整计划和策略

## 七、使用建议

### 7.1 何时启用计划管理功能

**推荐启用场景**：
- ✅ 多步骤的复杂验证任务
- ✅ 需要长时间执行的任务
- ✅ 需要人机协同的场景
- ✅ 使用 Enhanced/Advanced 交互模式

**可选场景**：
- ⚠️ 简单的单步任务
- ⚠️ 快速原型验证
- ⚠️ 标准模式下的实验性使用

### 7.2 配置方式

**方式 1：命令行参数**
```bash
# Standard 模式启用
ucagent output/ Adder --use-todo-tools

# Enhanced 模式（自动启用）
ucagent output/ Adder --interaction-mode enhanced
```

**方式 2：配置文件**
```yaml
# config.yaml
interaction_mode: "enhanced"  # 自动包含计划管理
```

**方式 3：MCP Server 模式**
```bash
ucagent output/ Adder --mcp-server --use-todo-tools
```

### 7.3 最佳实践

1. **合理设置步骤数量**：
   - 不要太细（<2步）：失去规划意义
   - 不要太多（>20步）：难以管理
   - 推荐：5-10步适中

2. **步骤描述要具体**：
   - ❌ 不好："做测试"
   - ✅ 好："为 add 函数编写10个正常输入测试用例"

3. **及时更新进度**：
   - 完成一个或多个步骤后立即调用 `CompleteToDoSteps`
   - 定期使用 `GetToDoSummary` 检查整体进度

4. **灵活调整计划**：
   - 发现问题时使用 `UndoToDoSteps` 或 `ResetToDo`
   - 不要僵化地执行初始计划

## 八、总结

计划管理函数（CreateToDo等）在 UCAgent 智能体执行过程中起到以下**核心作用**：

1. **任务组织器**：将复杂的芯片验证任务分解为结构化的执行步骤
2. **进度追踪器**：实时记录和显示任务执行状态，支持进度查询
3. **执行引导器**：在 Enhanced/Advanced 模式下主动引导智能体按计划执行
4. **上下文记忆**：减少对长对话历史的依赖，提供持久的任务上下文
5. **人机接口**：为人机协同验证提供清晰的沟通界面
6. **质量保证**：通过系统化的步骤管理，降低遗漏关键验证环节的风险

这些功能使 UCAgent 能够更有效地执行复杂的硬件验证任务，特别是在需要多步骤协调、长时间执行或人机协同的场景下，显著提升了验证的系统性、可控性和成功率。

## 九、参考代码位置

- 计划管理工具定义：`vagent/tools/planning.py`
- 智能体主类：`vagent/verify_agent.py`
- Enhanced 交互逻辑：`vagent/interaction/enhanced.py`
- 工具编排系统：`vagent/interaction/orchestrator.py`
- 阶段管理：`vagent/stage/vmanager.py`
- 配置文件：`vagent/lang/zh/config/default.yaml`

---

**文档版本**：1.0  
**生成日期**：2024-11-04  
**UCAgent 版本**：基于最新主分支分析
